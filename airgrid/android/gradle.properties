org.gradle.jvmargs=-Xmx8G -XX:MaxMetaspaceSize=4G -XX:ReservedCodeCacheSize=512m -XX:+HeapDumpOnOutOfMemoryError
android.useAndroidX=true
# This builtInKotlin flag was added automatically by Flutter migrator
android.builtInKotlin=false
# This newDsl flag was added automatically by Flutter migrator
android.newDsl=false
# Optimized resource shrinking. Flutter already sets minifyEnabled and
# shrinkResources for release (FlutterPlugin.kt), and AGP 8.11 defaults
# android.r8.integratedResourceShrinking and android.nonFinalResIds to true,
# which are this flag's three preconditions. It lets R8 shrink code and
# resources in one pass instead of resources being swept separately after.
# Play Console asks for it under "Improve your app's memory and performance
# with R8". Note: still FeatureStage.Experimental in AGP 8.11.1.
android.r8.optimizedShrinking=true
